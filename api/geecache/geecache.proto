syntax = "proto3";

package geecache.geecache;

option go_package = "geecache/api/geecache;geecache";

message Request {
  string group = 1;
  string key = 2;
}

message Response {
  bytes value = 1;
}

service GroupCache {
  rpc Get(Request) returns (Response);
}
